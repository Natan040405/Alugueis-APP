import 'package:flutter/material.dart';

class AptoPage extends StatelessWidget {
  const AptoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Aptos'));
  }
}