import 'package:flutter/material.dart';

class PredioPage extends StatelessWidget {
  const PredioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Predio'));
  }
}